name: fx_core
description: Core models, configuration, and utilities for the fx monorepo tool.
version: 0.1.0
publish_to: none

resolution: workspace

environment:
  sdk: ^3.11.1

dependencies:
  meta: ^1.9.1
  path: ^1.9.0
  yaml: ^3.1.2
  yaml_edit: ^2.2.1

dev_dependencies:
  lints: ^6.1.0
  test: ^1.24.0

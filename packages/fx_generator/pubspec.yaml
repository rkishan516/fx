name: fx_generator
description: Code generation framework for the fx monorepo tool.
version: 0.1.0
publish_to: none

resolution: workspace

environment:
  sdk: ^3.11.1

dependencies:
  fx_core:
    path: ../fx_core
  path: ^1.9.0
  yaml: ^3.1.2

dev_dependencies:
  lints: ^6.1.0
  test: ^1.24.0

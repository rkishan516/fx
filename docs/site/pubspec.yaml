name: fx_docs
description: Documentation site for fx — a Dart/Flutter monorepo management tool
version: 0.0.1

environment:
  sdk: ^3.11.1

resolution: workspace

dependencies:
  jaspr: ^0.22.3
  jaspr_content: ^0.5.0
  jaspr_router: ^0.8.1

dev_dependencies:
  build_runner: ^2.10.0
  build_web_compilers: ^4.4.15
  jaspr_builder: ^0.22.3
  lints: ^6.1.0

jaspr:
  mode: static
